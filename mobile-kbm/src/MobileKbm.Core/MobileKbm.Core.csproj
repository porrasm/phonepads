<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Everything that is not Windows: schemas, gestures, key state and keeping the session
    alive. The tray app supplies the one Windows piece, an IInputSink over SendInput.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\shared\Phonepads.Protocol\Phonepads.Protocol.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="MobileKbm.Tests" />
  </ItemGroup>

</Project>
